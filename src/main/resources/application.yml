spring:
  threads:
    virtual:
      enabled: true
  ai:
    mcp:
      server:
        name: math-calculator
        version: 1.0.0
        type: SYNC
        sse-message-endpoint: /mcp/message
        annotation-scanner:
          enabled: false

server:
  port: 44321
